name: "CI/CD - ai-tutor audit (dev)"

on:
  push:
    branches: [main]
    paths:
      - services/audit/**
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: false
        default: "manual deploy"

permissions:
  contents: write
  security-events: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/shared-deploy.yaml
    secrets: inherit
    with:
      service: "audit"
      port: "8012"
      ecr_repo: "opsera/ai-tutor-audit"
      placeholder_key: "PLACEHOLDER_ECR_URI_AUDIT"
      dockerfile_pattern: "service"
      contract_service_name: "audit"
