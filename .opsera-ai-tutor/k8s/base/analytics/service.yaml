apiVersion: v1
kind: Service
metadata:
  name: analytics
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: analytics
spec:
  selector:
    app: analytics
  ports:
    - name: http
      port: 8011
      targetPort: 8011
  type: ClusterIP
