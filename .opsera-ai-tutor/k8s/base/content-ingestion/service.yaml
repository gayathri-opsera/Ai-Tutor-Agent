apiVersion: v1
kind: Service
metadata:
  name: content-ingestion
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: content-ingestion
spec:
  selector:
    app: content-ingestion
  ports:
    - name: http
      port: 8003
      targetPort: 8003
  type: ClusterIP
