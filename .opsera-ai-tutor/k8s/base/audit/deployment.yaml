apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: audit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit
  template:
    metadata:
      labels:
        app: audit
        app.kubernetes.io/name: ai-tutor
        app.kubernetes.io/component: audit
    spec:
      serviceAccountName: ai-tutor
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: audit
          image: PLACEHOLDER_ECR_URI_AUDIT
          ports:
            - containerPort: 8012
          envFrom:
            - configMapRef:
                name: ai-tutor-config
            - secretRef:
                name: ai-tutor-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8012
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8012
            initialDelaySeconds: 30
            periodSeconds: 30
