apiVersion: v1
kind: Service
metadata:
  name: audit
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: audit
spec:
  selector:
    app: audit
  ports:
    - name: http
      port: 8012
      targetPort: 8012
  type: ClusterIP
