apiVersion: v1
kind: Service
metadata:
  name: chat-orchestrator
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: chat-orchestrator
spec:
  selector:
    app: chat-orchestrator
  ports:
    - name: http
      port: 8004
      targetPort: 8004
  type: ClusterIP
