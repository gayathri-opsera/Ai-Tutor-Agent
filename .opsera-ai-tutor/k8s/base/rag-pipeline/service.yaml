apiVersion: v1
kind: Service
metadata:
  name: rag-pipeline
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: rag-pipeline
spec:
  selector:
    app: rag-pipeline
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
