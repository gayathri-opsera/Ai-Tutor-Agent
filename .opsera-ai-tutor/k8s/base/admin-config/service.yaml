apiVersion: v1
kind: Service
metadata:
  name: admin-config
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: admin-config
spec:
  selector:
    app: admin-config
  ports:
    - name: http
      port: 8009
      targetPort: 8009
  type: ClusterIP
