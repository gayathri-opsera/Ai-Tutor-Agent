apiVersion: v1
kind: Service
metadata:
  name: learner-profile
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: learner-profile
spec:
  selector:
    app: learner-profile
  ports:
    - name: http
      port: 8008
      targetPort: 8008
  type: ClusterIP
