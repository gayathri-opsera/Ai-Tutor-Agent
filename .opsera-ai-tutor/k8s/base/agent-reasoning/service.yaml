apiVersion: v1
kind: Service
metadata:
  name: agent-reasoning
  labels:
    app.kubernetes.io/name: ai-tutor
    app.kubernetes.io/component: agent-reasoning
spec:
  selector:
    app: agent-reasoning
  ports:
    - name: http
      port: 8005
      targetPort: 8005
  type: ClusterIP
